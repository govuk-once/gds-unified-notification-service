# Terraform DynamoDB Module

This module provides a standardized way to deploy Amazon DynamoDB tables. It enforces a strict naming convention and ensures consistent tagging across all project resources.

- Features
  - Standardized Naming: Automatically prefixes tables with the project name
  - Tag Injection: merges global repository tags with resource-specific tags
  -
